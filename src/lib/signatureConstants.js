/** Default signature display size in the cover letter (px). */
export const SIGNATURE_WIDTH = 200;
export const SIGNATURE_HEIGHT = 72;
/** How far the signature descends over the printed name (at default height). */
export const SIGNATURE_NAME_OVERLAP = 20;

export const SIGNATURE_MIN_WIDTH = 80;
export const SIGNATURE_MAX_WIDTH = 360;
export const SIGNATURE_MIN_HEIGHT = 28;
export const SIGNATURE_MAX_HEIGHT = 140;

/** Large canvas for the draw-signature modal. */
export const SIGNATURE_MODAL_DISPLAY_WIDTH = 560;
export const SIGNATURE_MODAL_DISPLAY_HEIGHT = 200;
